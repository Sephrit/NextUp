# Optional: only needed by Watchmode-powered MCP tools outside the desktop app.
# Never commit a real value. The desktop app stores its key in the OS credential vault.
NEXTUP_WATCHMODE_KEY=

# Optional override when an MCP client cannot find the desktop app's data folder.
NEXT_UP_DATA_DIR=
